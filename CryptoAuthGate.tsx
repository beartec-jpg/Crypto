// Existing code that checks if the environment is development
const isDevelopment = window.location.hostname.includes('localhost') || window.location.hostname.includes('replit') || window.location.pathname.includes('/dev');

// Additional code logic (if any)