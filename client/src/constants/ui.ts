// UI-related constants for CryptoSandbox component

// Chart margins
export const MARGIN = { top: 20, right: 80, bottom: 40, left: 20 };

// Toolbar and controls dimensions
export const LEFT_TOOLBAR_WIDTH = 60; // Left toolbar width in pixels
export const TOP_CONTROLS_HEIGHT = 60; // Top controls area height in pixels

// Zoom scale limits
export const ZOOM_SCALE_EXTENTS = [0.5, 20] as const; // [min, max] zoom scale
