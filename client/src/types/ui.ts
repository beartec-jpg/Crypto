// UI-related types for CryptoSandbox component

// Dimensions interface
export interface Dimensions {
  width: number;
  height: number;
}

// Menu position interface
export interface MenuPosition {
  x: number;
  y: number;
}

// Bounds interface
export interface Bounds {
  left: number;
  right: number;
  top: number;
  bottom: number;
}
